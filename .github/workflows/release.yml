name: Release

on:
  push:
    branches: [main]
  pull_request:
    types: [labeled, unlabeled]

permissions:
  contents: write
  pull-requests: write
  issues: read

jobs:
  release:
    uses: ./.github/workflows/reusable-release.yml
